# Swift / Xcode
.build/
.xcode-build/
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Model files (too large for git)
*.bin
Sources/VocaMac/Resources/models/*.bin

# IDE
.idea/
.vscode/
*.code-workspace

# Swift Package Manager
Packages/
.swiftpm/
# Package.resolved is deliberately tracked: release builds resolve from a clean
# checkout, so the lockfile is what makes a tagged build reproducible.

# Built app bundle
VocaMac.app/

# Distribution artifacts
dist/

# Legacy stubs
Sources/CWhisper/
VocaMac.iconset/

# Bitbucket Pipelines (lives only on Bitbucket's release branch, not GitHub)
bitbucket-pipelines.yml

# Tool artifacts
.desloppify/
.claude/
VocaMac.app.bak/

# Local build/test artifacts
*.profraw
web/cookies.txt
